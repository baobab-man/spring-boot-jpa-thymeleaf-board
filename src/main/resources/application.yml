spring:
  datasource:
    url: jdbc:mariadb://minokim.synology.me:3306/dev
    username: embedded
    password: Springboot12#
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
    database:

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
